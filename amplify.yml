version: 1
frontend:
  phases:
    build:
      commands:
        - ls -al
        - ls -al static-html-page
  artifacts:
    files:
        - index.html
    # Folder from submodule
    baseDirectory: static-html-page/src
